# BDD-style scenarios per feature

Every non-trivial feature lives here as a Markdown file with Given/When/Then
scenarios. The CLAUDE.md spec-first / test-first rule (§20.3) applies:
implementation comes after the scenario.

Use `_TEMPLATE.md` as the starting point.
