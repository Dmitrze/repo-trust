//! Threshold tables loaded from `config/default.toml` at runtime.
//!
//! Thresholds are versioned with the scoring model.  See
//! `docs/scoring-model.md` for the change log.

// TODO: load TOML at runtime via figment.
