# Phase H + I — Final pre-flip checklist + flip к public

After Phase G (release `v0.1.0` created, tag pushed) — this is the
last sanity check before `gh repo edit --visibility public`.
**8 checks + flip command + immediate post-flip verification**.

⏱️ **Total ~15 minutes**.
